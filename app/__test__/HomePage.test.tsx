import { render, screen } from "@testing-library/react"
import HomePage from "@/pages/index";

describe("Testing integration home Page", () => {
  it("should be find app name text", async () => {
    
    // render(<HomePage />)

    // const aboutText = await screen.findByText(/WhatsApp Book/i)

    // expect(aboutText.outerHTML).toBe("<span>WhatsApp Book</span>")
  })

})